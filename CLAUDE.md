# Project memory for Claude Code — Make My Dreams (MMD)

> Claude Code auto-loads this file at the start of every session opened in this repo. It propagates the project constitution to ANY skill, slash-command, sub-agent, or interactive task running in this session — not just to MMD's `auto-dev` engine.

## What this project is

Make My Dreams (MMD) is an accessibility and orchestration layer for AI-driven development. It stands on top of Spec Kit, OpenSpec, BMAD, gStack, and Ralph Loop rather than replacing them. The full design rationale lives in [`MAKE_MY_DREAMS.md`](./MAKE_MY_DREAMS.md) (16+ versions of evolving scoping) and the documented dev-by-AI problems in [`PROBLEMS.md`](./PROBLEMS.md).

## Constitution — modular, loaded by binding

The project constitution is modular (v2.0+). Modules live in `.specify/memory/constitution/`. A bindings table at `.specify/memory/constitution-bindings.yaml` tells which modules apply to a given skill, worker, engine, profile, or context.

**For an LLM working in this repo session:**

If you are about to invoke a known skill, worker, or engine, look up its required modules in `constitution-bindings.yaml` and load only those. This keeps your context lean.

If you are doing ad-hoc work and don't know which modules apply, load these baselines:

@.specify/memory/constitution/universal.md
@.specify/memory/constitution/ai-coding.md
@.specify/memory/constitution/commit-git.md

Then add as needed:
- Touching tests? `@.specify/memory/constitution/testing.md`
- Touching security-sensitive code? `@.specify/memory/constitution/security.md`
- Producing documentation? `@.specify/memory/constitution/documentation.md`
- Handling runtime errors? `@.specify/memory/constitution/error-handling.md`
- Making architectural decisions? `@.specify/memory/constitution/architecture.md`
- Working on a Kid-profile project? `@.specify/memory/constitution/safe-by-default.md` + `@.specify/memory/constitution/kid.md`

The full index of modules and their loading rules is in [`.specify/memory/constitution.md`](./.specify/memory/constitution.md).

## Constitution diffusion mechanisms

The constitution is propagated through three complementary layers:

**Layer A — Claude Code session (this file).** Anthropic auto-loads `CLAUDE.md` at session start. Any skill, slash-command, sub-agent, or interactive task spawned in this session inherits these instructions. Covers gStack skills (`/qa`, `/cso`, etc.), `/loop`, `/ralph-loop`, and Task-tool sub-agents.

**Layer B — auto-dev pipeline.** `install-mmd.sh` materializes the `auto-dev` workflow that explicitly injects the constitution into each Phase 1–4 sub-agent. Independent of this file; redundant for safety.

**Layer C — MMD CLI subprocess.** `lib/invoke-autodev.js` injects the constitution into the prompt passed to `claude -p` when MMD runs `auto-dev` headless (CI, cron, dream-bench). Required because subprocess `claude` sessions do not necessarily share the parent's CLAUDE.md.

The composer (`lib/constitution-compose.js`, **shipped v0.3.c**) turns the bindings table into a function that returns the per-invocation module bundle as a string ready for prompt injection. `parseBindings` (hand-rolled YAML-lite, no external dep) reads `constitution-bindings.yaml`; `resolveModules({profile}, bindings)` resolves `MMD_PROFILE` to `defaults.always ∪ profiles[profile]` (deduped, deterministic); `composeConstitution({profile})` reads each `.specify/memory/constitution/<name>.md` and concatenates them under per-module headers. `buildPrompt` injects that block when `MMD_PROFILE` is set — so a Kid build carries the real `safe-by-default.md` + `kid.md` text and a Pro build carries `pro.md` — and falls back to a minimal line if the bindings/modules are unreadable (graceful, never crashes). Currently composes by **profile** only; engine/context/skill dimensions are a future slice (the resolver is built to extend). See [ADR-024](./docs/adr/024-constitution-composer-layer-c.md).

## Working agreements in this repo

- **Branch first**: never commit directly to `main` for non-trivial work. See `commit-git.md` for naming conventions.
- **Push immediately**: after every commit, `git push`. Uncommitted-and-unpushed work doesn't exist.
- **Red-green for every failure**: any failure (test, install script, pipeline, integration) triggers a deterministic red→green test pass before it's considered fixed. See `testing.md`.
- **Test stratification**: tag every test you write (`@smoke`, `@unit`, `@integration`, `@e2e`). See `testing.md` §V.
- **AI honesty**: report walls explicitly, don't fabricate success. See `ai-coding.md` §I.
- **Sealed self-dev (optional, v0.4.b)**: a MMD slice MAY now be launched with `mmdream --here --sealed "<change>"`. An independent tester writes blind acceptance tests into the gitignored `.mmd/shared/sealed-tests/`, MMD seals them by hash, auto-dev implements on the slice branch, and MMD fails the slice (exit 6, files named) if any sealed test was weakened or deleted (anti-P-04, L-023). Use it when the slice's correctness must be guarded by an oracle the implementing agent cannot edit. See [ADR-026](./docs/adr/026-sealed-test-oracle.md). The sealed pipeline also logs a **blast radius** to `status.json.blast_radius`; as of v0.4.c this is **import-graph accurate** (not a grep stub) — `lib/sealed-tests/import-graph.js` parses + resolves module specifiers and records the transitive reverse closure (every file that imports a changed file directly or through a chain), with **zero new dependencies** (vanilla-stack) and a documented residual gap (computed/runtime specifiers, re-export aliasing, non-JS importers). See [ADR-027](./docs/adr/027-import-graph-blast-radius.md), L-024. As of **v0.4.d** the sealed pipeline adds a **behavioral judge** (`lib/sealed-tests/judge.js`, P-09) that runs **after** the deterministic sealed-test gate passes: a `claude -p` oracle grades the implementation against *what was asked* (the dream/ACs) and emits a tagged per-AC verdict. All ACs `MET` → proceed; any `NOT-MET`/`UNCERTAIN` (or an unparseable reply → the sacred `uncertain` fallback, never a fabricated `met`) → **exit 7 (behavioral-gap)**, slice not marked done, verdict written to `status.json.judge`. **Exit 7 is distinct from the tamper/seal exit 6** (a tampered seal still exits 6 *before* the judge runs), so "tests went red / check attacked" (6) and "tests green but behavior misses the ask" (7) are distinguishable. See [ADR-028](./docs/adr/028-llm-judge-behavioral-oracle.md), L-025.
- **Conductor notifications (opt-in, v0.5.a)**: a detached run (`setsid … mmdream --here "<change>"`) now **pushes** a signal when it ends instead of leaving you to poll. Set **`MMD_NOTIFY_URL`** (e.g. `https://ntfy.sh/<your-topic>`) and MMD POSTs a small JSON `{event,slice,state,summary,ts,message}` on run **done/failed** (`event ∈ {run_done, run_failed}`) to your own sink — ntfy/Slack/Discord/custom. It is **opt-in** (unset → no payload, no network call, default unchanged) and **best-effort** (a dead URL / non-2xx / timeout logs one line and is dropped — the run's exit code and `status.json` are **never** affected; the send is bounded by a ~5 s timeout). Payload is **run metadata only** — no secrets/env/file contents (least-disclosure egress to *your* sink). Pure core + injected sender in `lib/conductor/notify.js` (`shouldNotify` / `buildNotification` / `sendNotification`); wired into both the greenfield and `runHereMode` completion paths in `bin/mmd.js`. First brick of the v0.5 **Conductor**; the `stream-json` context monitor + 70% auto-handoff are deferred to v0.5.b (they change the spawn). See [ADR-029](./docs/adr/029-conductor-notifications.md), L-026.
- **Live context monitor (opt-in, v0.5.b)**: `mmdream --monitor "<dream>"` (and `mmdream --here --monitor`) makes the Conductor **see** the orchestrator's context filling up. It spawns auto-dev as `claude -p … --output-format stream-json --verbose`, parses each event's `usage` live (`input_tokens + cache_read_input_tokens + cache_creation_input_tokens`), looks up the window from the model id (a **`[1m]`** suffix → 1,000,000; known model → 200,000; unknown → 200,000 `estimated:true` — never a fabricated %), and writes the running **max** to `status.json.context = {model,window,tokens,pct,estimated}`. The tee is re-rendered **human-readable** (assistant text + periodic `[monitor] context X% (tokens/window)` lines, NOT raw JSON; `MMD_QUIET=1` respected). At **`MMD_HANDOFF_THRESHOLD`** (default `0.70`, custom value in `(0,1]` honored) it writes a `READY_FOR_HANDOFF` marker, logs a line, and fires a **`context_70`** notification **once** (debounced, reusing the v0.5.a `MMD_NOTIFY_URL` fan-out) — it does **not** stop the run (no auto-handoff yet). **Opt-in is safety-critical**: the default text spawn is the path `mmdream --here` uses to build MMD itself, so it stays **byte-for-byte unchanged** (the default args carry no `--output-format`, pinned by a test). It is the **orchestrator's** context, not per-sub-agent. Pure parse/math in `lib/conductor/stream-parse.js` (`parseStreamEvent`/`contextWindowFor`/`contextTokens`/`contextPct`); the monitored spawn + readable re-render in `lib/invoke-autodev.js` (`buildAutodevArgs`/`makeMonitorConsumer`); the status writes + 70% signal/notify in `bin/mmd.js`. Second brick of the v0.5 **Conductor**; the actual auto-handoff/resume stays future. See [ADR-030](./docs/adr/030-live-context-monitor.md), L-027.
- **Context gauge in `mmdream serve` (opt-in, v0.5.c)**: the v0.5.b monitor is now **visible** to the non-technical web user. `mmdream serve`'s dream form gains a **"Monitor context (advanced)"** checkbox; ticking it appends `--monitor` to the web launch and the progress view shows a live **context gauge** — a bar (% of the window), humanized `tokens / window` (`337000→337k`, `1000000→1.0M`), the model, a fixed 70% threshold marker, and a "⚠️ ready for handoff" badge iff `context.ready_for_handoff`. The page **polls** `GET /api/status/<slug>` every ~3 s while a monitored job runs and re-draws the gauge from the response's `.context` (polling over SSE-push so the gauge is **decoupled** — a failed/slow poll is swallowed and **never breaks the page or the SSE stream**). `GET /api/status/<slug>` now surfaces `status.json.context` (`{model,window,tokens,pct,estimated}` with the sibling `ready_for_handoff` folded into a boolean) — additive, omitted when absent, malformed status behaves as before. **Opt-in is the safety contract**: unticked (default) → no `monitor` in the launch body → spawn args **byte-for-byte today's** (no `--monitor`/`--output-format`, pinned by a test) → no gauge, no extra polling, SSE unchanged. Strict boolean validation (`monitor` junk → false) on both launch routes (`POST /api/dream` + the catch `/confirm`). Pure render in `bin/serve-ui/gauge.js` (`renderGauge`/`humanizeTokens`, also unit-tested in node); the status fold + `--monitor` threading (`buildMmdAutodevArgs`) + served `/gauge.js` in `lib/server.js`; the checkbox/gauge/poll glue in `bin/serve-ui/{index.html,app.js,style.css}`. Gauge markup is CSP-safe (native `<progress>` + external-CSS 70% marker; the page runs under `style-src 'self'`). Third user-facing step of the v0.5 **Conductor**; auto-handoff still future. See [ADR-031](./docs/adr/031-serve-ui-context-gauge.md), L-027.
- **Using MMD on your own repo (third-party readiness, v0.6.a)**: MMD now works on a repo other than itself, with **no new command to learn**. `mmdream discover` **names the stack it scanned** — a real Node/Python/Go/Rust app reports the new **`brownfield-app`** case (`lib/discover/classify.js`), distinct from `blank` (a genuinely empty/unstructured repo; `already-onboarded`/`rich`/`bmad-alone` still win, malformed input still degrades to `blank`). And the first `mmdream --here` on an unprepared repo runs a **transparent first-run setup guard** (`lib/onboarding/`: pure `detectMmdSetup` probe + injected-dep `runFirstRunSetup` + pure `buildOnboardingCheatsheet`), wired into `runHereMode` right after the `Mode: --here` line and **before** the onboarding gate: **TTY** → one confirm (`Run setup now? [o/N]`) then spawn `install-mmd.sh <target>` + print the env-var cheat-sheet + proceed; **non-TTY** (`serve`/CI) → auto-run + honest log; **decline or runner failure** → **exit 8** (a new rung — NEVER an inert launch); **`MMD_SKIP_SETUP=1`** bypasses (mirrors `MMD_SKIP_GROUNDING`); **already-ready** → no-op and an **existing `.specify/memory/constitution.md` is never overwritten** ("elle reste"). The cheat-sheet surfaces the operational tribal knowledge (`MMD_TIMEOUT_MS=0`, the spec-frozen dream directive, commit-per-AC, `--sealed`/`--monitor`/`MMD_NOTIFY_URL`). The deeper "the build reads the *project's* constitution" composer rework (Layer C) is the deferred **v0.6.b**. See [ADR-032](./docs/adr/032-transparent-first-run-setup.md).
- **Considerate guest of a third-party constitution (v0.6.b)**: on a repo that **already has** a constitution, `mmdream discover` now adds an advisory **"Constitution suggestions"** section to `mmd-discovery-report.md` — a **deterministic, pure** keyword checklist (`lib/discover/constitution-suggest.js`: `suggestConstitutionImprovements(text) → {present[], missing[{theme,suggestion}]}`) over common governance themes (testing, commit/git, security, error-handling, design principles, documentation, AI-coding hygiene). It is **honest about being a heuristic** (the renderer labels it so, never an audit), pure/no-I/O/never-throws (empty/malformed → all-missing), and **never writes `constitution.md`** — *"elle reste"* (an integration test asserts the file is byte-for-byte unchanged across a discover run). No constitution present → the section is omitted (the v0.6.a first-run setup materializes a default instead); LLM-enriched suggestions are a deferred opt-in (`--suggest-with-claude`, YAGNI). `bin/discover.js` does the read + threads `constitutionText` into the pure `buildReport`. **The discover-then-`--here` friction is fixed**: discover gitignores its own scratch (`.mmd/`, `mmd-discovery-report.md`) via an idempotent marked block, and the first-run setup preflight in `bin/mmd.js` treats a tree dirtied **only** by MMD-managed paths (`.mmd/`, `mmd-discovery-report.md`, `.gitignore` — pure predicate in `lib/onboarding/mmd-managed.js`) as clean, so the documented flow needs no manual stash; **any non-MMD dirty path still refuses with exit 4** (v0.6.a F7 intact — the setup's `git add -A` can never sweep real user work). The **Layer-C composer rework was retired, not built** (honest correction, universal §VI): `--here` is already governed by the *project's* constitution via **Layer B** (the BMAD auto-dev workflow reads `.specify/memory/constitution.md` directly); the composer only runs on the greenfield `demo/` path where no project constitution exists. See [ADR-033](./docs/adr/033-constitution-suggestions-and-discover-friction.md).
- **Documentalist coherence review (`mmdream document-review`, v0.7.a)**: the first brick of the §6.4 **Documentalist**, built **detect-first and report-only**. `mmdream document-review` answers *"what did MMD design vs what did it become, and where have the docs drifted?"* by gathering a **deterministic inventory** of MMD's real surface (`lib/documentalist/inventory.js`: subcommands, git tags, ADRs+titles, `lib/` modules, per-doc line counts with a 200-line §6.4 cap flag, root `SPEC_V*.md` sprawl count, active-lesson count — injected fs readers, **never throws**, degrades each field to empty/null), **reconciling** it against the `MAKE_MY_DREAMS.md` §9 roadmap (`lib/documentalist/roadmap-reconcile.js`: a PURE `reconcileRoadmap({roadmapText,inventory})` that parses `### vX.Y — Title` headers and classifies each capability **built/partial/unbuilt** by name-matching against the inventory — a **clearly-labelled heuristic, not an audit**; conservative: single fully-matched concept → built, half-matched compound → partial, no signal → unbuilt; malformed → unknown, never throws), and **rendering** (`lib/documentalist/coherence-report.js`) the designed-vs-built table + doc-health flags (length-cap violations, SPEC sprawl) + inventory summary to markdown. The subcommand (`bin/documentalist/document-review.js`) writes **EXACTLY** `docs/coherence-review.md` (a regenerable, tracked dashboard with a "do not hand-edit" banner) and is **strictly read-only beyond that one file** (an integration test asserts `git status` shows only that path — the safety heart of detect-before-act). Opt-in **`--with-claude`** layers an LLM commentary pass via the `MMD_DOCUMENT_REVIEW_CMD` spawn seam (rendered under its own heading — it never mutates the deterministic table), with a **graceful honest fallback** to the deterministic report when claude is absent/non-zero/empty (never a fabricated classification — L-021, universal §VI). Dispatch mirrors the document-* contract (`bin/mmd.js` + USAGE + `SUBCOMMANDS`). The acceptance test is reproducing the manual gap audit: on MMD itself it correctly flags all six big rocks (auto-handoff, Dream Expander, Plan-Review, Bundle A Security, full Documentalist, polymorphic Reality Check) as unbuilt/partial, the `MAKE_MY_DREAMS.md` cap violation, and the SPEC sprawl. The roadmap was reordered so **v0.7.b deepens *detection*** (per-claim DRIFT/conformance — truth over tidiness) and **active compaction** (archive the root SPECs, shard the oversized docs) slides to **v0.7.c**, the *act* face granted once detection is trusted. See [ADR-034](./docs/adr/034-documentalist-coherence-review.md).
- **Documentalist DRIFT / conformance check (`mmdream document-review`, v0.7.b)**: the Documentalist's primary value is keeping docs **true**, not short. `mmdream document-review` gains a **"## Drift / conformance"** section that scans the operational truth docs and flags where what a doc *claims* no longer matches what was **built** — **detect-before-correct, flag-only (it NEVER edits a doc)**. Two new pure modules: `lib/documentalist/doc-refs.js` (`extractDocRefs(text)` — line-aware, never-throws extraction of the artifacts a doc CLAIMS exist: `lib/`/`bin/`/`test/`/`docs/` `.js`/`.md` paths, backticked `mmdream <subcommand>` mentions, `ADR-NNN`, bare `lib/<module>`; **distinct in purpose** from the grounding extractor `lib/here-mode/extract-file-refs.js`, which is input-focused and ignores `.js`) and `lib/documentalist/conformance.js` (`checkArtifactConformance` → dangling refs that don't resolve `{doc,line,ref,kind,reason}`; `checkFactConformance` → **bounded** stale-fact mismatches only — explicit `N subcommands/ADRs/(active) lessons` counts + "current/latest version X" vs the v0.7.a inventory, ignoring historical "as of vX"/"in v0.2.x" narrative — both pure, never throw, **labelled heuristic**). `lib/documentalist/coherence-report.js` renders the section (dangling references + stale facts + an opt-in `--with-claude` **semantic-drift** sub-pass via the `MMD_DOCUMENT_REVIEW_CMD` seam, **honest fallback** — never a fabricated "conformant"); `bin/documentalist/document-review.js` wires it, staying **strictly read-only beyond `docs/coherence-review.md`** (asserted). **Precision is the priority** (a drift section that cries wolf is useless): fenced code blocks + placeholder paths (`0NN-slug.md`) are skipped, subcommands are captured only inside inline code, fact conformance runs **only on the living current-state docs** (README/CLAUDE/HANDOVER — ADRs and `lessons-learned.md` are point-in-time records whose counts were true when written, so they are scanned for dangling *code* refs only), the subcommand authority is derived from the **real `bin/mmd.js` dispatch** (truth over tidiness — `mmdream lessons` is dispatched yet missing from the `SUBCOMMANDS` export), and **negated/hypothetical/future** command mentions ("no standalone `mmdream judge`", a "future `mmdream doctor`", the v0.7.c "`mmdream document-compact`" plan) are suppressed by a not-a-claim guard. On MMD itself it reports **3 dangling references + 0 stale facts** (two code files removed when `ship` was retired, still cited in historical records; one rejected-alternative `mmdream init` in an ADR), with **zero** false positives on the dozens of valid refs — and it dogfooded itself, catching two stale ADR counts in README/HANDOVER mid-slice (corrected, so the live scan is now clean). Auto-*correcting* drift and compaction stay future (v0.7.c). See [ADR-035](./docs/adr/035-documentalist-conformance-drift.md).
- **Documentalist active compaction (`mmdream document-compact`, v0.7.c)**: the Documentalist's first **action**. v0.7.a *detected* the root `SPEC_V*.md` sprawl and v0.7.b *checked* the docs' truth; `mmdream document-compact` clears the sprawl by archiving the root SPECs into `docs/specs/` with an index and rewritten references — **act on the safe thing before the hard thing** (archiving is mechanical + fully reversible via `git mv`; the higher-risk **semantic** compaction — sharding the over-cap docs — is deliberately **deferred** until SPEC archival is trusted). A **pure planner** `lib/documentalist/compact.js` (`planCompaction({specs, existingArchive}) → {moves, indexMarkdown, referenceRewrites}` — `dst` under the `docs/specs/` folder, `indexMarkdown` newest-first by parsed version with each SPEC's title line, `referenceRewrites` mapping each root `SPEC_V0XX.md` token to its archived counterpart — plus `applyReferenceRewrites`, an **idempotent** exact-token transform that prefixes a reference **only when not already prefixed**, so no `docs/specs/docs/specs/`; both pure, no I/O, never throw, empty specs → empty plan, already-archived SPECs not re-planned). The subcommand `bin/documentalist/document-compact.js` does the I/O: `git mv` each root SPEC into `docs/specs/` (history preserved — `git log --follow` reaches the original), write a newest-first index there, and rewrite references (link targets, anchored links, prose mentions, backticked links) across tracked markdown **outside** the archive (a moved SPEC keeps its bare sibling cross-refs). **Safety contract**: move-only (**never** edits doc prose, never deletes), **idempotent** (no root SPECs → a clean no-op; second run changes nothing), **reversible**, preconditions validated **before any mutation** (a non-git repo → exit 5, an untracked SPEC → exit 6, nothing half-applied), and it does **not** auto-commit. `--dry-run` prints the plan and changes nothing (clean tree after). **Validated by its own Drift detector** (the v0.7.a/b synergy): after compaction a `mmdream document-review` must show **no new dangling SPEC references**. New SPECs still land at the repo root per slice; a later run archives the shipped ones (the §6.4 periodic-consolidation model), so shipped SPECs live under `docs/specs/`. The live 34-SPEC archival on MMD is the operator's **post-merge** step (the slice validates on **fixture** git repos so it never moves its own in-flight `docs/specs/SPEC_V07C.md`). Dispatch mirrors the document-* contract (`bin/mmd.js` + USAGE + `SUBCOMMANDS`). See [ADR-036](./docs/adr/036-documentalist-active-compaction.md).
- **Documentalist coherence graph — staleness-on-diff (`mmdream document-review --since <ref>`, v0.7.d)**: the structural answer to "change one of doc/code/ADR, know the other two need a look." `mmdream document-review --since <ref>` takes the slice's diff (`git diff --name-only <ref>`), walks a **derived** file-level coherence graph from the changed files, and prints a ranked, **advisory** **"## Coupled changes"** report to **stdout** — *"change one node, learn which neighbors to review"*. **Derive, never maintain** (the golden rule — a hand-kept map rots): the graph is composed from edges that already exist for free — `computeBlastRadius` (ADR-027) gives code↔code import edges, the v0.7.b `lib/documentalist/doc-refs.js` gives doc→code refs, and the new doc-links extractor gives doc↔doc. Two new **pure** modules: `lib/documentalist/doc-links.js` (`extractDocLinks(text, {docPath, resolveAdr})` → doc↔doc edges: `[[wiki]]` links, `ADR-NNN` resolved to the real ADR file via an injected resolver else a number-keyed stem, relative `.md` links; conservative — external/absolute/anchor/code links yield no edge; never throws) and `lib/documentalist/coherence-graph.js` (`buildCoherenceGraph({importEdges, docToCodeEdges, docLinkEdges})` → a **file-level, bidirectional** adjacency, each edge keeping its `kind` import/doc-ref/doc-link; `coupledNeighbors(graph, changedFiles)` → per changed file, neighbors **ranked strong (direct) before weak (2-hop transitive)**, deduped, excluding the changed set; pure, deterministic, never throws). The `--since` mode in `bin/documentalist/document-review.js` is a **standalone READ-ONLY query**: it returns before any roadmap read or report write — it does **NOT** rewrite `docs/coherence-review.md` (asserted: clean tree after a run), and the no-flag dashboard path stays byte-for-byte unchanged; a bad/unknown ref or non-git repo → honest **exit 5**, `--since` with no value → exit 2. **Precision-first** (the v0.7.b discipline — a graph that cries wolf is useless): edges are kept **only to real tracked files** (no phantom neighbors), and **hub suppression** means a high-degree node (CLAUDE.md/HANDOVER.md reference dozens of ADRs) is reported as a direct neighbor but **not** used as a transit node for weak edges, so a change near it doesn't weak-flag the whole repo. **Advisory + ranked, never a hard gate** (coupling ≠ certainty — review, don't obey). It is exactly what would have caught the v0.7.c coupled-test break (the moved SPECs ↔ the inventory test) *before* it went red. Symbol-level granularity, the semantic `@mmd:link` anchor, the Mermaid/.dot mind-map, and git co-change edges are deliberately **deferred** (file-level + the diff check is ~80% of the value). See [ADR-037](./docs/adr/037-coherence-graph-staleness.md).
- **Coherence graph — cap the hub-SOURCE flood (`mmdream document-review --since`, v0.7.e)**: a precision refinement to the v0.7.d staleness report. v0.7.d already suppressed hub *transit* (it does not traverse weak/transitive edges THROUGH a high-degree doc like CLAUDE.md/HANDOVER.md, so a change *near* a hub doesn't flag the whole repo). v0.7.e adds the symmetric face: when the file a diff changed **is itself the hub** (you edit CLAUDE.md), every one of its dozens of direct references used to be reported as `strong`, flooding the "Coupled changes" report with 40+ lines — which, per the v0.7.b discipline, flags nothing. Now the pure `coupledNeighbors` (`lib/documentalist/coherence-graph.js`) detects a **hub source** (direct degree > `HUB_DEGREE`, the existing threshold, now exported), **caps it to the top `HUB_DEGREE` ranked direct neighbors** (cut taken AFTER the deterministic strong→kind→path sort, so the kept slice is the highest-ranked), **records the dropped count** on the entry (`hubSuppressed`, added ONLY when capping — an ordinary entry keeps its exact `{file, neighbors}` shape, back-compat), and **skips the transitive layer** for it (weak edges atop a flood are pure noise). `renderCoupledChanges` (`bin/documentalist/document-review.js`) prints an **honest** `… +N more direct neighbors suppressed (hub source — …)` note — never a silent truncation (universal §VI). A non-hub `--since` run is **byte-for-byte v0.7.d's** (asserted). Both faces reuse the one `HUB_DEGREE` lever: a hub couples to too much to be an actionable hint, whether it is *near* the change or *is* the change. Per-kind/configurable thresholds stay deferred (YAGNI). See [ADR-038](./docs/adr/038-coherence-graph-hub-source-cap.md).
- **`/mmdream` operator slash command (v0.7.5)**: the MMD operator playbook, encoded as a Claude Code slash command so a user — or Claude — can drive MMD by intent from a session without remembering the CLI incantations or the hard-won operational rules. `/mmdream <intent>` instructs the session to **route** the intent: **(a) a code change for the current repo** → launch `mmdream --here` **detached** via `setsid` (L-001) with every operational rule applied (`MMD_TIMEOUT_MS=0` so the 30-min default can't kill a real slice — L-016, bun + node 20 on `PATH`, a human-readable `--label` slug — universal §VII, `MMD_DREAM_MAX_LEN=4000`, the dream told to **commit incrementally per AC** — L-019, and the spec-frozen directive when a frozen SPEC is cited), then set up a **per-run watcher** and monitor honestly via `status.json` state + git commits — NOT the buffered redirect log (L-002, `claude -p` doesn't flush in real time), reporting honestly (ai-coding §I) and, on green, **offering** (never silently performing) a fast-forward-only merge + an annotated tag; **(b) a greenfield dream** → `mmdream "<dream>"`; **(c) a bare subcommand** (`discover`, `serve`, `document-review`, `handover`, …) → `mmdream <subcommand>` directly. It is **a prompt template, not a CLI change** — no `mmdream` code is touched. The single source of truth is the **tracked** file `assets/claude-commands/mmdream.md` (deliberately NOT under the gitignored `.claude/`, so it's reviewable, versioned, and anchor-tested — L-009/L-023 design-vs-substrate discipline); `install-mmd.sh` materializes it **idempotently** into `.claude/commands/mmdream.md` next to the `bmad-adv-auto-dev` command (source dir from `BASH_SOURCE`, `cp` overwrite, honest warn + skip if the source is absent — universal §VI). `@unit` anchor tests pin every operational-rule anchor; an `@integration` test drives the install step on a temp target (byte-for-byte materialization, idempotency, skip-when-absent). See [ADR-039](./docs/adr/039-mmd-operator-slash-command.md).
- **Test Curator — `mmdream test-health` (v0.7.6)**: the test analog of the Documentalist, but for **tests not docs** — and **DISTINCT** from the two roles it keeps getting confused with: `mmdream qa` reviews *a single change* (per-diff correctness, fix-oriented); the BMAD TEA (Murat) designs *test architecture* for a feature ahead of time. `mmdream test-health` owns the fourth question neither answers: **is the test _corpus itself_ healthy as it grows?** It is **detect-and-report only** (strictly read-only — it **NEVER modifies a test**) and **deterministic** (no `claude` spawn — the corpus signal is exactly computable, so an LLM would add cost + non-determinism for no gain; contrast `document-review --with-claude`). It gathers the git-tracked test files (`git ls-files '*.test.js'`, **excluding `test/fixtures/`** — those are inputs to the discover tests, not MMD's own corpus), scans each test's **title** for the stratification tag (`@smoke`/`@unit`/`@integration`/`@e2e` prefix, the same convention `npm test:smoke` greps + testing.md §V mandates), and writes EXACTLY `docs/test-health.md` (the test analog of `docs/coherence-review.md`; an `@integration` test asserts no other tracked path changes — the read-only safety heart, mirroring `document-review`). The report surfaces the **stratification distribution**, the **untagged tests** (a §V violation, listed with `file:line` — the first live run on MMD flagged **73** real ones in older files), a **smoke-health line** (count vs the §V 5–10 fast-feedback band — thin/usable/over-budget), and **oversized files** (over `MMD_TEST_FILE_MAX_LINES` default 500 OR `MMD_TEST_FILE_MAX_TESTS` default 60 — env-overridable, validated with graceful honest fallback) as split candidates. Honest advisory framing throughout, clearly a **heuristic** not an audit. Detect-before-act: it **names** the debt (e.g. the 73 untagged) but never auto-retags — a wrong stratum is worse than an absent one (it corrupts the fast lane), so closing the debt is a separate human-reviewed action (the same earn-trust-then-act arc the Documentalist took in v0.7.a→c). Pure `scanTestCorpus` + `buildTestHealthReport` in `lib/test-curator/{scan,report}.js` (never throw, deterministic); the thin coordinator in `bin/test-curator/test-health.js`; dispatch + USAGE + `SUBCOMMANDS` mirror the document-* contract. See [ADR-040](./docs/adr/040-test-curator-test-health.md).
- **Test Curator redundancy face — `mmdream test-health` (v0.7.7)**: the Test Curator's **second face**, answering *which tests likely **overlap**, so the corpus can be pruned?* (ADR-040 deferred "duplicate-test detection"; now built). Same contract as the stratification face — **deterministic** (no `claude`), **advisory**, and **DETECT-BEFORE-CUT**: it **NEVER deletes a test** (a similar-looking test may document a distinct intent — the **human decides**). Method = **clustering by target + structural similarity, NOT coverage** (a coverage mode needs an instrumented run — non-deterministic, breaks the read-only/pure contract — so it stays a **deferred opt-in**). The report's new **"Redundancy candidates"** section lists (a) **near-duplicate test pairs** — each test's *body* (the code between the callback braces, captured by a deterministic brace-depth scan that skips strings/comments in `lib/test-curator/extract-bodies.js`) is normalized (comments+whitespace stripped, tokenized), turned into k-gram **token shingles**, and scored by **Jaccard similarity** ≥ `MMD_TEST_DUP_SIMILARITY` (default **0.9**, in (0,1], graceful fallback); and (b) the **most-tested modules** as **over-test candidates**, clustering test files by the `lib/`/`bin/` modules they import (extracted per file). **Bounded** — pairs are compared **within-file only** (same file ⇒ same target cluster, so no global quadratic blow-up over ~1760 tests); the cluster view needs **zero** pairwise work. **Precision-first** (no crying wolf): the high 0.9 default + a small token floor (trivial stub bodies skipped) yield **3** real pairs on MMD (0.91–0.95), not a flood; the most-tested module is `lib/invoke-autodev.js` (204 tests / 33 files). Long lists are capped with an **honest "+N more"** note (no silent truncation, universal §VI). Documented heuristic residual: an import line written inside a *fixture string* in a test file is counted as a target (same class as the v0.7.6 `test(`-in-a-string note). Pure `nearDuplicatePairs` + `targetClusters` in `lib/test-curator/redundancy.js`; `scan.js` attaches `body`+`targets` additively; `report.js`+the bin render the section and stay **strictly read-only beyond `docs/test-health.md`** (asserted). See [ADR-041](./docs/adr/041-test-curator-redundancy-detection.md).
- **Test Curator goes POLYGLOT — `mmdream test-health` (v0.8.0)**: the fix for a **correctness failure** — the Test Curator was **secretly JavaScript-only** (it hard-wired `test()`/`it()`, the `@`-tag convention, `import`/`require` + `lib/`/`bin/` targets, brace bodies, `*.test.js`), so on a **Rust/Python/Go/C** repo it found nothing or **fabricated** numbers — a direct violation of MMD's any-technology mission and the brand-new **constitution §VIII (technology-agnostic analysis, NON-NEGOTIABLE)**. The fix is MMD's **"orchestrate, don't reimplement"** shape: a **generic, language-neutral core** + per-technology **adapters**. **The core imports NO adapter and contains NO language syntax** (`lib/test-curator/redundancy.js` similarity/clustering, `lib/test-curator/report.js` render — they operate ONLY on normalized entries). **Adapters** live in `lib/test-curator/adapters/`: `index.js` is the **documented contract** (`id`, `displayName`, `matches(signals)`, `discoverTests({repoRoot,files,readFile})` → normalized entries `{file,line,title,stratum|null,body|null,targets|null}` + per-file metrics + **capability flags** `supportsBodies`/`supportsStratification`/`supportsCoverage`) + the **pure registry** (`resolveAdapters(signals)` returns ALL matching adapters from manifest presence — `package.json`→JS, `pyproject.toml`/`setup.py`/`requirements.txt`→Python, `Cargo.toml`→Rust, `go.mod`→Go — `[]` when none; plus `detectStackNames` naming even unsupported stacks and `supportedStackNames`). `adapters/javascript.js` is the **existing** scan/extract/`@`-tag/target logic behind the contract — **zero behavior change** for a JS repo (MMD self-run is byte-identical: 1774 tests, 187 files, same strata/bodies/targets/pairs/clusters; only the `tag`→`stratum` rename + genericized prose differ), with all JS assumptions (and its JS-internal `scan.js`/`extract-bodies.js`) living HERE. `adapters/python.js` is the **proof of genericity** — discovers pytest/unittest `def test_*` (functions + methods), pytest-marker strata, `import`/`from` project targets; declares **`supportsBodies:false` honestly** (clustering + stratification still work, redundancy pairs marked **not-available** — never a silent ✅-none). **Dependency direction: core ← adapters ← `index` ← bin.** The bin (`bin/test-curator/test-health.js`) runs the **§VIII honest gate**: detect the stack(s) → run every matching adapter → aggregate → write `docs/test-health.md`; when **NO adapter matches**, **REFUSE honestly** — name the detected stack + supported list, **exit 6, NO report, NO fabricated numbers** (the rule that stops the JS-on-Rust bug); a **mixed** repo analyzes the supported stacks and **names** the unsupported ones. **Coverage stays deferred for ALL adapters and will be polyglot** (each adapter's native tool → lcov/cobertura — NOT `node --test` baked into the core). The same §VIII reckoning is queued for blast-radius / coherence-graph / doc-refs (also JS-specific). See [ADR-042](./docs/adr/042-polyglot-test-curator-adapters.md).
- **Import graph goes POLYGLOT — blast-radius + coherence-graph code edges (v0.8.1)**: the second §VIII reckoning, paying down the debt ADR-042 named. MMD's **code-dependency analysis** was still secretly JavaScript-only: `computeBlastRadius` (`lib/sealed-tests/import-graph.js` + `blast-radius.js`, ADR-027) parsed **JS** `import`/`require`/`export … from`/dynamic-`import()` specifiers, and the **coherence graph**'s `--since` code↔code edges (`lib/documentalist/coherence-graph.js`, ADR-037) are built on it — so on a **Rust/Python/Go/C** repo the blast radius (which the **sealed-test gate** consumes) and the coherence edges were **wrong or empty, silently**. Same "orchestrate, don't reimplement" shape as the Curator, but a graph spans a **mixed** repo so the honesty is **per-file**, not whole-repo-refuse. New **`lib/code-graph/adapters/`**: `index.js` is the **import-edge adapter contract** (`id`, `matches(filePath)` **by extension** — a per-file decision, NOT a manifest — and `importEdges({filePath,content,repoFiles})` → resolved repo-relative imported paths, externals/unresolvable **dropped**, never throws) + the **registry** (`adapterFor(filePath)` → adapter|null, `unanalyzedLanguageFor` → the language name for a recognized **code** file with no adapter, scoped to source extensions so a JS-only repo's list is never flooded by `.md`/`.json`). `adapters/javascript.js` is the **EXISTING** specifier-parse + `./`/`../`/`+ext`/`/index` resolution moved behind the contract with **ZERO behavior change** — `computeBlastRadius` on MMD returns the **identical** reverse closure and the **sealed gate is unaffected** (the AC-2 regression lock); all JS specifier syntax lives HERE. `adapters/python.js` is the **proof of genericity** — resolves `import a.b` / `from a.b import x` to repo files via dir/package/`__init__.py` heuristics (stdlib/external dropped). The **core is language-neutral**: `lib/sealed-tests/import-graph.js`'s new `buildForwardGraph` dispatches each file to its adapter **via the registry** (the core imports **NO specific adapter**) and returns `{graph, unanalyzed}`; `reverseClosure` is unchanged pure graph math; `computeBlastRadius` gains an **additive** honest **`unanalyzed`** list (files whose language has no adapter — they contribute no edges and the result **says so**, never a silently-incomplete graph passed as complete — §VI/§VIII). **Dependency direction: core ← adapters ← registry ← callers.** `document-review --since` selects code files via the registry (JS+Python) and **honestly notes** any un-adapted language in the diff ("code coupling for `<stack>` not available — no import adapter yet"); an all-JS diff (MMD itself) is **byte-for-byte unchanged**. Validated: a fixture **Python** repo → real reverse closure, a **mixed** JS+Python repo → both, a **Rust** file → `unanalyzed`, no fake edges. **Zero new deps** (the L-024/YAML-lite bar — regexes + heuristics, not an AST). Remaining §VIII debt: the **doc→code ref extractor** (`doc-refs.js`, lighter) then **coverage** (polyglot when built), both queued in HANDOVER. See [ADR-043](./docs/adr/043-polyglot-import-graph.md).
- **Doc→code refs go POLYGLOT — the drift detector sees any language (v0.8.2)**: the third §VIII reckoning ADR-042/043 queued — the **Documentalist's doc→code reference extractor** (`lib/documentalist/doc-refs.js`, ADR-035 / v0.7.b), the input half of the drift / conformance detector. Its `FILE_PATH` matcher was **secretly JavaScript+MMD-only**: it captured a code-file reference ONLY under `lib/`/`bin/`/`test/`/`docs/` ending in `.js`/`.md`, so on a **Python/Rust/Go/Java/…** repo a doc citing `src/foo.py` or `src/main.rs` was **invisible** — the detector silently reported a clean bill it had no basis for (the §VIII silent-empty failure, §VI honesty too). The fix broadens `FILE_PATH` to a **path-like token with a directory segment + one of the common SOURCE/doc extensions** (`js|ts|jsx|tsx|mjs|cjs|py|rs|go|java|rb|c|h|cpp|hpp|cc|cs|php|swift|kt|scala|md`) under **ANY top-level directory** (not only `lib|bin|test|docs`), preserving every lookbehind/lookahead boundary (a path is never glued onto a preceding word; a leading-dot dir like `.specify/memory/x.md` is **not** half-captured; a `https://` URL is **not** captured; a bare `main.py` with no directory is **not** a claim). The MMD-specific patterns (`mmdream <subcommand>`, `ADR-NNN`, `lib/<module>`) are **unchanged** — conventions, not language-bound; they simply don't match on a non-MMD repo, which is correct. **PRECISION IS HELD by two derived guards** (the §VIII priority: no new false positives on a real repo): (1) a **pure structural guard** rejecting a source extension in a NON-final path segment (`…\.<ext>\/…`) — a slash-joined prose LIST like `CLAUDE.md/HANDOVER.md` or `kid.md/pro.md` is not a real path (whose only extension is terminal); (2) a **repo-rooted resolution filter** in `lib/documentalist/conformance.js` (injected `repoTopDirs`) — a `file` ref is judged dangling ONLY when rooted at a **real top-level directory of the analyzed repo** (`bin/documentalist/document-review.js` derives the repo's actual dirs), the **derived, not-hardcoded successor to the old `lib|bin|test|docs` allowlist** (same precision, any layout: `src/`/`app/`/`cmd/`/`internal/`…). A non-rooted token — a shorthand like `adapters/javascript.js` (real path `lib/…/adapters/…`), a relative markdown link `adr/020-x.md`, an illustrative `pkg/mod.py` — is not a claim about THIS repo's exact path, so it's skipped (absent/empty `repoTopDirs` → filter OFF, back-compat judge-all). The extractor stays **pure** (candidates broad); precision is enforced downstream where the repo is known (the v0.7.b "candidates then resolve" shape). The broadening surfaced **six** would-be false positives on MMD's own docs; both guards remove all six **without editing a single shipped doc**, so MMD's drift report is unchanged (the same 4 pre-existing baseline refs it already tolerated, **zero** new false positives). `--since` is unaffected (its doc→code edges already resolve via exact `trackedSet.has`). Validated by a **Python+Rust fixture** integration test (missing `src/foo.py`/`src/main.rs` flagged; existing `src/real_module.py`/`src/lib.rs` not; non-rooted + slash-joined not new FPs) + `repoTopDirs` unit tests. **Zero new deps** (regex + one `readdirSync`). Remaining §VIII debt: **coverage** (deferred for all adapters; polyglot when built). See [ADR-044](./docs/adr/044-polyglot-doc-code-refs.md).
- **The autolearning loop is CLOSED — validated-reuse counter + LLM promotion gate (v0.9.0)**: differentiator #2's hole, finally sealed. The composer auto-injects matched lessons (v0.2.e) and `mmdream document-lessons` increments each lesson's `**To promote if**: N (counter: c)` line and auto-promotes into a constitution module at threshold (v0.2.i) — **but the counter rose on RAW INJECTIONS** (`counter + injCount`), which ADR-010 itself had already named the wrong signal ("the composer matching keywords is not the same as a validated re-use"). So a lesson that merely *appeared* in prompts climbed toward MMD's own constitution with no evidence it ever *helped*. v0.9.0 swaps the signal and gates the promotion. **(1) The counter now rises on a deterministic VALIDATED REUSE** = a lesson **injected into a run that completed `state=done`**, counted **once per distinct run** (a weak-but-honest, cheap, reproducible proxy; a lesson injected only into **failed** runs gets nothing). Pure `validatedReuses(records)` in `lib/autolearn/validated-reuse.js` (per-run dedup, never throws, empty→empty). **The correlation crux** — composer.json records *which* lessons were injected but not the run's final state, and status.json is overwritten every run — is solved durably: every run writes a sibling `<runId>.outcome.json {run_id,state,ts}` under `.mmd/local/runs/` (`lib/autolearn/run-outcome.js`, written at EVERY completion path in `bin/mmd.js` — `--here`/sealed/greenfield, done and failed), sharing one basename = the `runId` (also stamped into composer.json); `lib/composer/usage-stats.js` `buildRunRecords` joins audits→outcomes. **Idempotent**: `lib/documentalist/mutate-counters.js` increments by validated reuses **not-yet-credited**, recording credited runs in `.mmd/local/credited-runs.json` so re-running `document-lessons` never double-counts a run; `--dry-run` prints per-lesson deltas, writes nothing. **(2) Promotion into the constitution is GATED by an injected LLM validation** (`lib/autolearn/promote-gate.js`, pure `parsePromoteGateVerdict` → `validated|not-validated|uncertain` + prompt builder; mirrors the v0.4.d judge — MMD dictates the `VERDICT:` format, parser never throws). `document-lessons` runs the gate (the `MMD_PROMOTE_GATE_CMD` `claude -p` seam) on each threshold lesson reviewing the rule + its reusing runs: **ONLY an explicit `validated` promotes** (append to the module + remove from `lessons-learned.md`); `not-validated`/`uncertain`/unparseable/spawn-error/timeout/**gate-absent** all → **HOLD** (lesson stays active, counter preserved, an honest `held: gate <verdict>` note) — the **sacred fallback, NEVER a fabricated constitution change** (universal §VI). The gate runs strictly *behind* the deterministic counter, never instead of it. **(3) Raw injections vs validated reuses are surfaced DISTINCTLY** — `mmdream lessons` gains a **VR** column beside **INJ**, and `document-lessons` prints both totals on separate labelled lines, so the ADR-010 confusion can never silently recur. **Archival** (lesson unused M months → archived) is the deferred §6.5 tail. See [ADR-045](./docs/adr/045-autolearning-loop-closed.md).
- **`mmdream secret-scan` — the first Bundle A Security brick (v0.9.1)**: a security **GATE** for autonomous dev — catch a leaked credential **before** it is committed (MMD runs an agent that writes + commits for 30–90 min; a key in a fixture or a checked-in `.env` is one `git push` from being public forever). `MAKE_MY_DREAMS.md §6.6` names Bundle A Security; a pre-commit secret gate is its cheapest, highest-leverage first brick (`mmdream cso` only *discusses* secret scanning via its LLM — it never deterministically gated). **Vanilla + language-agnostic + read-only.** The pure core `lib/security/secret-scan.js` (`scanText(text, opts) → {rule, line, column, redactedMatch, confidence}`, never throws) detects high-confidence formats — **private keys** (`BEGIN … PRIVATE KEY`), **AWS** (`AKIA`+16), **GitHub** (`ghp_`/`gho_`/`ghs_`/`ghr_`/`github_pat_`), **Slack** (`xox<letter>-`), **Google** (`AIza`), **JWT** (three base64url segments) — plus a **generic high-entropy assignment** (a secret-like identifier assigned a quoted value whose Shannon entropy ≥ threshold and length ≥ min). **No external tool (trufflehog), no new dependency — regex + entropy only** (the L-024 vanilla-stack bar). **§VIII by construction, NOT via adapters**: scanning for secrets is polyglot *by nature* — a secret is a textual pattern, not a language construct — so a `.py`/`.rs`/`.env`/`.yaml`/`.txt` are all scanned identically with **no per-language adapter** (the honest reading of §VIII here; contrast the Test Curator/import graph which parse language *structure* and so DO need adapters). **REDACT-NEVER-ECHO** (universal §VI + `security.md` least-disclosure): every finding shows only a few leading chars then asterisks — the full secret is never returned, logged, or printed, so the scanner can't become a second disclosure path. **PRECISION-FIRST** (secret scanners are a false-positive nightmare): obvious placeholders/examples (`EXAMPLE`/`xxxx`/`0000`/`your-token-here`/`redacted`/`changeme`) are skipped, an inline **`mmd-secret-ok`** comment on the same or preceding line whitelists a known-safe fixture (substring check — language-agnostic), and the entropy rule needs both a secret-like identifier AND a high-entropy long value so prose + base64 images don't trip it. The read-only gate `bin/security/secret-scan.js` has three surfaces — default (all git-tracked text files; binary detected by a NUL byte + skipped, gitignored excluded by `git ls-files`), **`--staged`** (the staged blobs via `git show :path` — the pre-commit surface), **`--since <ref>`** — and **GATES**: the six format rules are `confidence:'high'` and a high-confidence finding **exits 1**; the `generic-high-entropy` heuristic is honestly **`medium`** = printed *advisory*, does NOT change the exit code (gating on the FP-prone rule would train people to bypass the hook — L-023's "fight the harness"; a `--strict` mode is a deferred opt-in). It writes **nothing** (asserted by a clean `git status` test). Dispatch + USAGE + `SUBCOMMANDS` mirror the document-*/test-health contract. Exit codes: `0` clean · `1` gate · `2` argv · `5` not-a-git-repo/bad-ref. **Opt-in pre-commit hook** (AC-4): `install-mmd.sh` always materializes a **non-active** sample at the gitignored `.mmd/hooks/pre-commit` (idempotent, sentinel-bounded for a test to extract, mirroring the `/mmdream` materialization) but **never enables it without you** — `MMD_INSTALL_SECRET_HOOK=1` installs it into `.git/hooks/pre-commit` and even then **refuses to clobber** an existing hook. Dogfood: `mmdream secret-scan` on MMD itself scans 527 tracked files **clean** (exit 0). Follow-up Bundle A bricks (deferred): a dependency/slopsquatting **deps-gate** and an **egress-sandbox**. See [ADR-046](./docs/adr/046-secret-scan-gate.md).
- **`mmdream deps-gate` — the second Bundle A Security brick (v0.9.2)**: a security **GATE** that catches a **poisoned dependency before it is installed** — the supply-chain analog of secret-scan. MMD runs an autonomous agent that decides which third-party libraries to pull in, and the dominant attack is **typosquatting / slopsquatting** (`reqeusts` for `requests`, `lodahs` for `lodash`, or a name an LLM is likely to *hallucinate*): a one-keystroke-off package whose install script ships malware. `mmdream cso` only *discusses* dependency risk via its LLM; nothing in MMD **deterministically verified a dependency before it landed**. `mmdream deps-gate` reads a project's declared dependencies and, per dependency, asks its ecosystem registry *"does this exist, how old is it, how adopted is it, and is its name suspiciously close to a popular package?"* **§VIII by ADAPTERS, NOT by construction** (the inverse of secret-scan): dependency declaration + registry metadata are genuinely **per-ecosystem**, so deps-gate is the adapter-based §VIII shape (like the Test Curator ADR-042 / import graph ADR-043) — a **pure language-neutral risk core that imports NO adapter and contains NO ecosystem syntax** (`lib/security/deps-assess.js`: `assessDependency({name,declared,metadata,popularNames,opts}) → {name,findings}`, never throws, plus `gateExit`) + **per-ecosystem adapters** (`lib/security/deps-adapters/`: `index.js` contract + pure registry `resolveAdapters`/`detectStackNames`/`supportedStackNames`; `npm.js` parses `package.json` deps/devDeps and hits registry.npmjs.org/api.npmjs.org; `python.js` parses `requirements.txt`+`pyproject.toml` and hits pypi.org/pypistats — the two real adapters; cargo/go are **named** but unbuilt → detect-and-refuse). Dependency direction: **core ← adapters ← registry ← bin**. **PRECISION-FIRST** (a deps gate that cries wolf gets bypassed — L-023): exactly **two** findings GATE (`severity:'high'`) — `unresolvable` (registry has no such package) and `likely-typosquat`, the **CONJUNCTION** (near a popular name AND brand-new AND barely-downloaded, all three: `MMD_DEPS_TYPO_DISTANCE` default 2, `MMD_DEPS_MIN_AGE_DAYS` default 30, `MMD_DEPS_MIN_DOWNLOADS` default 1000); each single signal alone (`near-popular-name`/`very-new`/`low-adoption`) stays **medium advisory** and does NOT change the exit (mirrors secret-scan's "format rules gate, generic-entropy stays advisory"). **NETWORK HONESTY (§VI)**: the fetch lives ONLY in the bin behind an **injected `fetchJson` seam** (real `fetch` in the bin, a fake map in tests so the suite is fully **offline**), is **bounded** (~5 s/request, concurrency 6, capped at 200 deps/run logged honestly), and a failed/timed-out/offline fetch → an honest **`unverified` medium advisory + exit 0** — **never a fabricated pass, never a network-blink hard-block**. The read-only gate `bin/security/deps-gate.js` has two surfaces — default (all declared deps across supported manifests) and **`--since <ref>`** (only deps ADDED in `git diff <ref> -- <manifest>`, the real supply-chain surface) — names which registries it queried (explicit egress), labels itself a **heuristic not an audit**, and writes **nothing** (clean `git status` asserted). When the ONLY manifest present is an unsupported stack (e.g. `Cargo.toml`-only) → **refuse honestly** (exit 6: name the stack + supported list, NO report, NO fabricated numbers — the rule that stopped the JS-on-Rust bug); a mixed repo analyzes the supported stacks and **names** the unsupported ones. Exit codes mirror secret-scan: `0` clean/advisory-only/nothing-to-gate · `1` gate · `2` argv · `5` not-a-git-repo/bad-ref · `6` unsupported-only refuse. **Zero new dependencies** (regex + `JSON.parse` + a hand-rolled bounded Levenshtein + Node's built-in `fetch` — the L-024 vanilla bar; no `semver`, no TOML lib, no registry client). **Opt-in pre-commit wiring**: the v0.9.1 sample hook now ALSO runs `mmdream deps-gate --since HEAD` (after secret-scan; falls back to a full scan on the first commit when there's no HEAD); `MMD_INSTALL_DEPS_HOOK=1` mirrors `MMD_INSTALL_SECRET_HOOK=1` (still never auto-enabled, still refuses to clobber). Dispatch + USAGE + `SUBCOMMANDS` mirror the secret-scan contract. Follow-up Bundle A bricks (deferred): cargo/go adapters (one file each via the registry), known-CVE/advisory lookup, install-script analysis, a `--strict` mode, response caching, and brick 3 the **worker-sandbox egress allowlist**. See [ADR-047](./docs/adr/047-deps-gate-supply-chain.md).
- **Technology-agnostic greenfield generation + honest preview (v0.10.a)**: the §VIII reckoning applied to the **GENERATION** path (not just analysis), and the biggest user-visible fix in the backlog. The greenfield build prompt (`lib/invoke-autodev.js buildPrompt`) was **hardcoded for the original v0.1 drawing-camera demo** — it injected, for ANY dream, three lines forcing `vanilla HTML/CSS/JS + Canvas API + getUserMedia`, the fixed `index.html/style.css/app.js/manifest.json` file list, and a camera-permission safe-default. For a PDF editor (Sébastien's first real `mmdream serve` build) that was **actively wrong**: it forced a camera, forbade `pdf-lib`/`pdf.js`, imposed a file layout that didn't fit. Never caught because we only dogfooded the drawing-camera demo (the hardcoded case) and `--here` (which short-circuits `buildPrompt`). The fix: **stop imposing a stack** — the agent **DERIVES** the simplest stack/structure/entry from the scope in `.mmd/shared/slice.md`; MMD injects only KISS, the profile/Layer-C block (preserved byte-for-byte), and a **SOFT** (never hard) preference for a no-build browser-previewable web app *when the dream allows it*. The same camera hardcoding was ALSO removed from the `slice.md` template (`lib/parse-dream.js initStateFiles`) — the very file the derive-directive points at, so the scope the agent reads is no longer camera-biased. The agent writes a machine-readable **run descriptor** at `.mmd/shared/run.json` (`{kind: web-static|cli|service|library|other, entry, run}`) so the preview path no longer **guesses** the stack. **The honesty couple (§VI — do NOT ship the prompt alone)**: if only `buildPrompt` changed, a non-web dream would *build* but then Reality Check would FAIL on a missing `index.html` and serve would open a phantom `…/index.html` link — making serve **worse** and *pretending*. So the slice ships the prompt change **WITH** honest degradation of both preview surfaces (§VIII detect-and-refuse on the preview side): a new pure, never-throws reader (`lib/greenfield/run-descriptor.js`: `readRunDescriptor` → `{kind,entry,run}|null`; `isWebPreviewable(descriptor,demoDir)` → web-static-with-real-entry OR back-compat bare `index.html`); **Reality Check** (`lib/reality-check.js`) consults it before opening `index.html` and returns `{status:'SKIPPED', reason}` naming the kind + how to run it for a non-web build — **NEVER a FAIL** on a missing `index.html` (web builds + the `--here` short-circuit unchanged); **serve** (`lib/server.js` pure `buildPreviewResult`) emits in the `done` event either today's preview URL (web — unchanged) or an honest `{previewable:false, resultUrl:null, kind, runInstruction, message}` "Built a `<kind>` project — browser preview not available yet. To run it: `<run>`", which the serve UI (`bin/serve-ui/app.js`) surfaces instead of a broken link (producer + consumer shipped together, L-022). **Soft web preference, not none**: the greenfield/serve audience is a possibly non-technical person who will try to open the result in a browser, and a no-build static app is the only thing today's preview can show — so the nudge stays, but it NEVER overrides fitting the dream (a PDF editor and a bulk-renamer CLI are both first-class). **Agent-written descriptor, not heuristic file-sniffing** (guessing the stack from produced files is the §VIII anti-pattern; the agent knows what it built). Zero new deps (`JSON.parse` + `existsSync` — the L-024 bar). **Scope = slice 1 of 3**: this slice is tech-agnostic prompt + descriptor + honest *degradation*; (2, deferred) Reality Check adapter-based *real* verification per stack (CLI→exit-code, service→health-check); (3, deferred) serve preview adapter-based *richness* (web→iframe, non-web→render artefacts). A re-dreamt drawing-camera demo still yields a camera app — because the agent derives it, not because it's hardcoded. See [ADR-048](./docs/adr/048-technology-agnostic-greenfield-generation.md).
- **Alignment gate on the NORMAL run path + iterate-on-gap (v0.11.a)**: the Conductor's first real CONTROL brick — "verify the ask, then correct" (task A of the A→E orchestrator sequence). MMD already had a behavioral judge that grades an implementation against WHAT WAS ASKED (`lib/sealed-tests/judge.js` — `buildJudgePrompt`/`parseJudgeVerdict`/`judgeFallback`, pure — + `invokeJudge` in `bin/mmd.js`, with the over-eager `OVERALL: MET` downgrade + the sacred `uncertain` fallback, P-09/L-025), **but it ran ONLY behind `--sealed`**. So a normal `mmdream --here`/greenfield run had **ZERO** automated alignment verification — correctness rested entirely on the operator running `npm test` + reading the diff. v0.11.a wires that **existing** judge onto the **normal** path (non-sealed `--here` + greenfield), **default-on**, as a gate that runs **after** auto-dev completes and **before** the run is marked done. **Reuse, don't reinvent**: `buildJudgePrompt`/`parseJudgeVerdict`/`judgeFallback`/`invokeJudge` + the OVERALL-met downgrade are reused **verbatim** (no second judge); `buildJudgePrompt` needs a `sealedDir`, so on the normal path the real evidence (the slice diff for `--here`, the produced files for greenfield) rides in `artifactsSummary` led by an **honest** "evidence is the diff, not a sealed suite" note, recomputed each judge call so a post-iteration diff is graded fresh. A new **pure** module `lib/conductor/alignment-gate.js` (never throws): `aggregateAlignment(verdict)` → `{aligned, gapAcs}` (aligned ⟺ `overall === 'met'`; `gapAcs` = the **not-met** ACs only — an `uncertain` AC is NOT a gap item, so the caller reads the three branches off `(aligned, gapAcs.length)`), `buildGapFeedback({gapAcs, dream})` → a correction-pass fragment restating the goal at both ends (counters constraint decay, ai-coding §III) + naming each unmet AC, and `parseMaxIters`. The iterate **loop** lives in `bin/mmd.js` (`runAlignmentGate`) where the seams are; the module only **decides**, never acts. **Default-on with an exact opt-out**: `MMD_SKIP_ALIGN=1` skips the gate and restores pre-v0.11 behavior **exactly** (mirrors `MMD_SKIP_GROUNDING`); the gate is a **post-completion step** — it **never** touches `buildAutodevArgs`/the spawn (the bootstrap/`--monitor` byte-for-byte contract, pinned by a `@unit` test). **Bounded iterate-on-gap**: on a NOT-MET verdict the gate re-launches auto-dev via the injected `invokeAutodev` seam with `buildGapFeedback` appended, up to `MMD_ALIGN_MAX_ITERS` (integer ≥ 0, default **1**, graceful fallback; `0` = gate-but-never-iterate), re-judging after each; a gap surviving the last attempt → **exit 7** (the EXISTING behavioral-gap code, reused), `status.json.judge` records the gap, the slice is **NOT** marked done. **The sacred fallback, distinct from a gap**: an `uncertain`/unparseable/gate-absent verdict (`gapAcs` empty but not aligned) → **no** blind iterate, **no** fabricated pass — an honest "alignment unverified — <why>" note, verdict recorded, the change on the branch but its alignment explicitly *unconfirmed* (universal §VI). Sealed paths are untouched (they already judge — no double-judge). The rest of the Conductor sequence is deferred: **B** externalized `decisions.log`/`handoff/` + the Orchestrator delegation loop, **C** auto-handoff@70%, **D** parallel Conductor + worktrees, **E** Bundle C observability; the **polymorphic Reality Check** (the deterministic/tool alignment face) complements this semantic/judge face — also deferred. See [ADR-049](./docs/adr/049-alignment-gate-normal-path.md).
- **The stateless, resumable orchestrator + REAL `--resume` (v0.12.a)**: the Conductor's **step B** pivot — make the auto-dev orchestrator *survivable*. `/bmad-adv-auto-dev` is MMD's own orchestrator skill (the `install-mmd.sh` heredoc); it already delegates each of its 4 phases to a fresh sub-agent, **but ran in ONE `claude -p` process with NO externalized phase checkpoint** — a kill (crash, Ctrl-C, reboot, or the coming 70%-context handoff) lost the whole orchestration and a rerun **restarted from Phase 1, re-doing the spec**, while MMD's `--resume` was a **stub** (printed state, exit 3 — continued no work). This violated the §4.2 stateless property ("any level can be killed and recreated from the files"). v0.12.a closes it in three **purely additive** pieces (a fresh no-checkpoint run is **byte-for-byte** unchanged): **(1)** checkpoint primitives `lib/conductor/checkpoint.js` (injected-fs, **never throws**): `writeCheckpoint(dir, {lastCompletedPhase, specFrozen, specPath})`, `readCheckpoint(dir)` → object or `null` (missing/malformed → null), `writeHandoffNote(dir, n, text)` → `handoff/<n>.md`, `isResumable(checkpoint, {totalPhases})` → `1 ≤ lastCompletedPhase < totalPhases`, and the pure `decideResume(...)` → three closed outcomes `relaunch | complete | none`. The files live under the **gitignored, run-local `.mmd/local/`** area (run STATE, not a deliverable — auto-dev's per-AC commits never sweep them); the **on-disk format is snake_case** (`last_completed_phase` …) = exactly what the LLM heredoc writes by hand, and `readCheckpoint` normalizes it to the camelCase JS API so both producers round-trip. **(2)** The auto-dev heredoc now **checkpoints each phase transition** (after Phases 1-4: write `handoff/<N>.md` + overwrite `checkpoint.json` with `last_completed_phase`/`spec_frozen`/`spec_path`; spec marked frozen at/after Phase 2) and is **resume-aware on init** (a new "Resume Check" reads the checkpoint; if phases are done it **announces** the resume, recovers from the `handoff/<N>.md` notes + the branch commits, and **starts at `last_completed_phase + 1`** — skipping completed phases, **never re-opening a frozen spec**; no checkpoint → today's fresh path). **(3)** A **REAL** `--resume` in `bin/mmd.js` replacing the stub: `mmdream --here --resume` (recovers the run from cwd's `status.json` — no dream arg) and greenfield `<dream> --resume` detect an incomplete run (resumable checkpoint AND not done) and **relaunch a fresh auto-dev via the existing `invokeAutodev`** on the same slice/demo with a resume-aware prompt, continuing from the checkpoint; a **complete** run or **no checkpoint** → an **honest no-op** (exit 0, "nothing to resume" / "no resumable run found"), **never a fabricated continuation** (§VI). E7 symlink defense preserved; `MMD_TIMEOUT_MS=0` honored on a resumed run (L-016). **Testability boundary (§VI)**: auto-dev's behavior is a *prompt* we cannot unit-test — so the tests cover the checkpoint primitives (pure, never-throw, boundary), the CLI resume orchestration (fake auto-dev relaunch), and that `install-mmd.sh` **materializes** the per-phase checkpoint-write + resume-aware-init instructions (grep the generated `workflow.md`); the end-to-end "really resumes mid-pipeline" is the explicit operator/live **AC-6**. This is the **foundation for step C** (auto-handoff@70% will *act* on the v0.5.b `READY_FOR_HANDOFF` marker by triggering exactly this resume against a fresh successor). Deferred: step C itself, sub-phase checkpointing (granularity is the phase boundary — KISS), cross-machine resume, steps D/E. See [ADR-050](./docs/adr/050-stateless-resumable-orchestrator.md).
- **Cooperative auto-handoff at 70% (v0.13.a)**: the Conductor's **step C** — it finally **ACTS** on the 70% signal it has been able to **SEE** since v0.5.b. Two pieces existed but were never wired: the v0.5.b context monitor (writes `ready_for_handoff` + a `context_70` notify at `MMD_HANDOFF_THRESHOLD` — **but did nothing**) and the v0.12.a resume (relaunches a fresh auto-dev continuing from the checkpoint). C wires them with a new **`mmdream --here --auto-handoff "<dream>"`**: at 70% MMD's monitor writes a small **handoff-request marker** (`.mmd/local/handoff-request`, gitignored run state); the auto-dev orchestrator, at each phase boundary **right after** writing its v0.12.a checkpoint, checks the marker and — if present — **announces the handoff and EXITS cleanly without starting the next phase** (leaving the incomplete checkpoint); MMD sees the clean exit + incomplete checkpoint + marker and **relaunches a fresh successor in resume mode** (continue from phase N+1 with low context). **Cooperative, NOT a forced kill** — the orchestrator stops only at a boundary it already reached and checkpointed, so no in-phase work is lost; the 0.70 threshold is conservative to leave headroom. **Opt-in by construction**: `--auto-handoff` **implies `--monitor`** (the context usage only exists in the stream-json spawn); WITHOUT the flag the default text spawn — the path MMD uses to build **itself** — is **byte-for-byte unchanged** (pinned), and the marker is written ONLY under `--auto-handoff` (so plain `--monitor`/a non-flag run **never sees a marker** and the phase flow is unchanged). **Bounded + honest**: capped by `MMD_MAX_HANDOFFS` (default 3); at the cap MMD launches **one final successor with handoff DISABLED** (runs to done/fails naturally) + logs the cap honestly (universal §VI) — progress is never infinitely deferred. A `handoff` notification fires per handoff (reusing the v0.5.a fan-out). **Reuse, don't reinvent** (DRY): no second resume or spawn path — it reuses v0.12.a's `invokeAutodev` + `buildResumeFeedback` + `readCheckpoint` and v0.5.a's notify. New code: a **pure** `lib/conductor/handoff.js` (`decideHandoff({checkpoint, handoffRequested, handoffsSoFar, maxHandoffs, totalPhases})` → `{action: finish|handoff|cap-final, reason}` — `finish` when complete OR no request, the safe default that prevents loops; `parseMaxHandoffs` → integer ≥ 1, default 3, graceful fallback — never throws); the marker primitives in `lib/conductor/checkpoint.js` (`writeHandoffRequest`/`readHandoffRequest`/`handoffRequested`/`clearHandoffRequest`, injected-fs, idempotent, never throw); `makeContextMonitor`'s optional `requestHandoff` callback + the `runHandoffLoop` in `bin/mmd.js` (wraps both `runHereMode` and greenfield; the v0.11 alignment gate runs ONCE on true completion, not per successor); the **HANDOFF CHECK** boundary-stop instruction in the `install-mmd.sh` heredoc; a `handoff` event in `notify.js`; `HANDOFF_FLAGS` in the argv parser. **Testability honesty (§VI)**: auto-dev's boundary-stop is a *prompt* — we test the instruction is **materialized** (grep the generated `workflow.md`), the pure decision logic (every branch + cap boundary + null-safe), the marker primitives, the handoff LOOP (a deterministic fake auto-dev simulating a cooperative stop → relaunch resume; the cap → final un-handoffed run; completion → finish), the monitor writing the marker at 70% end-to-end (the stream-json fake), and the default-spawn-unchanged pin; the real "a live run crosses 70% and hands off mid-pipeline" is the explicit operator/live **AC-6**. Deferred: a forced-kill fallback (when a phase overruns the window before a boundary), sub-phase granularity, adaptive thresholds, parallel handoff (step D), Bundle C observability (step E). See [ADR-051](./docs/adr/051-cooperative-auto-handoff.md).
- **HYBRID auto-handoff — incite, then ENFORCE at a checkpoint (v0.14.b)**: the live-finding fix for step C. Cooperative-only (v0.13.0) **did not work**: a real Sonnet `--auto-handoff` run wrote per-phase checkpoints yet ran past **≥2 boundaries to ~120% context with 0 handoffs** — a `claude -p` agent has **no reliable self-terminate primitive** and, under "finish the task" pressure, ignores the "exit cleanly at the boundary" instruction (the suite was green only because the fakes *simulated* the exit — green tests, broken reality; only the live run caught it). The fix Sébastien chose is **HYBRID** — keep the gentle path, don't depend on it: **(1) Incite (unchanged, Path A)** — the monitor still writes the `handoff-request` marker at the threshold and the heredoc still asks the orchestrator to stop at the next boundary; if it OBEYS → clean exit → MMD relaunches resume (the v0.13.0 path), no force. **(2) Enforce (new backstop, Path B)** — if the agent IGNORES the incitation (it advances the checkpoint a NEW phase while OVER the threshold and is STILL ALIVE), MMD waits a short **grace** (`MMD_HANDOFF_GRACE_MS`, default 15s — a last chance to exit on its own), then **terminates the orchestrator process GROUP (SIGTERM→SIGKILL) AT that checkpoint** and relaunches resume. **Why the enforce is safe** (the "killing worries me" concern): the kill happens ONLY at a phase checkpoint — the phase just COMPLETED and its per-AC commits (L-019) are already in git, so **no committed work is lost**; only the un-checkpointed reasoning since the last boundary, which the fresh successor re-derives. It is exactly §4.2 *"any level can be killed and recreated from the files"*, done at a safe point, as a backstop, after asking nicely first. **REUSE, don't reinvent**: the monitor (v0.5.b), `readCheckpoint` + resume relaunch (v0.12.a), `decideHandoff` + its v0.13.1 resumable-checkpoint gate, and the `handoff` notify are all reused — this slice ADDS only an **abort seam** + `shouldForceHandoff` + the grace, and REWIRES the loop to "cooperative-exit OR enforce-after-grace". New code: a **pure** `shouldForceHandoff({pct,threshold,lastCompletedPhase,phaseAtSpawn,handoffsSoFar,maxHandoffs})` → true iff `pct ≥ threshold` AND `lastCompletedPhase > phaseAtSpawn` (a NEW boundary since THIS successor launched — not the inherited checkpoint, preventing an immediate re-kill) AND under the cap (never throws — junk → safe `false`); `parseHandoffGraceMs` (non-negative int, 0 honored); an opt-in **abort seam** on `invokeAutodev` (`abortPredicate(ctx)` checked each monitor tick → on fire, grace then terminate the **process group** of a detached spawn — never orphaning auto-dev's `claude -p` children — and resolve a distinct `{aborted:'handoff', code:null}`; a cooperative exit DURING the grace resolves normally so the post-exit `decideHandoff` owns Path A; **inert + undetached without a predicate** → the default build-MMD spawn is byte-for-byte unchanged); and the `bin/mmd.js` wiring (`makeForceHandoffPredicate` captures `phaseAtSpawn` at launch + live `pct` + polled `readCheckpoint`; `runHandoffLoop` treats an aborted result as a handoff trigger — NOT a failure — routing it through the SAME `decideHandoff` path, threading `handoffsSoFar`+`phaseAtSpawn` so the cap-final successor is **un-enforced for free**; honest log wording distinguishes a clean stop from an ENFORCED terminate). The **heredoc incitation is KEPT as-is** (per-phase checkpoint + marker-check cooperative exit + resume init) — the enforce is purely MMD-side; an `@integration` test asserts the materialized workflow still contains all three and never asks the orchestrator to self-terminate. Bounded by `MMD_MAX_HANDOFFS` then one final un-enforced run; the v0.11 alignment gate runs ONCE on true completion; **v0.13.1 preserved** — a run that crosses the threshold but reaches no new boundary is never terminated. **Testability honesty (§VI)**: the enforce (SIGTERM at a checkpoint) is deterministic + fully tested (pure gate, the abort seam via a stay-alive stream-json fake, the hybrid loop via an enforce fake that ignores the incitation, the heredoc materialization); the **RESUME after a forced kill is still LLM-dependent and NOT yet proven live → AC-5 (a real forced-then-resumed run) is REQUIRED** — a resume failure is a reported wall and the slice is NOT done (never a green-by-fakes pass — the exact trap that hid the cooperative bug). Deferred: enforce mid-phase (before any checkpoint), sub-phase granularity, adaptive thresholds, the transparent flip (SPEC_V014A, until this is proven live), parallel handoff (D), Bundle C (E). See [ADR-053](./docs/adr/053-hybrid-auto-handoff-enforce.md).
- **The TRANSPARENT Conductor — monitor + hybrid auto-handoff default-on (v0.15.a)**: the flip the v0.14.b ADR deferred "until proven live" — now that the hybrid is **proven live** (cooperative + enforce, both paths, AC-5), make it the **DEFAULT** instead of opt-in behind `--auto-handoff`/`--monitor`. The Conductor's job (vision §4.2) is to be **INVISIBLE** — but a non-technical `serve`/Dream-Catcher user will **never** pass a flag, so opt-in meant the people who most need an invisible context manager never got one. v0.15.a is a **DEFAULTS flip + a single opt-out + back-compat handling — NO handoff logic is re-implemented**: the entire v0.14.0 machinery (the loop, `decideHandoff` + its v0.13.1 gate, `shouldForceHandoff`, the abort seam, the monitor, the resume relaunch, the v0.11 alignment gate that still runs ONCE on true completion) is **reused UNCHANGED**. **(1)** `buildAutodevArgs`'s `monitor` param now defaults to `true`, so the default CLI args INCLUDE `--output-format stream-json --verbose`; `monitor:false` is the byte-for-byte historical shape. **(2)** a new **pure** `resolveConductorMode(env)` (`lib/argv-parser.js`) returns `{monitor,autoHandoff}` — default both `true`; the **single opt-out `MMD_NO_AUTO_HANDOFF=1`** → both `false` (restores the pre-v0.15 behavior EXACTLY: plain text spawn, one un-looped invocation, no monitor, no loop — the bootstrap/cost escape hatch). It reads ONLY the env, so the legacy `--auto-handoff`/`--monitor` flags become **accepted-but-inert no-ops** (kept in `KNOWN_FLAGS` so old scripts never hit an "unknown flag" error; they parse and change nothing). **(3)** `bin/mmd.js` dispatch resolves the conductor mode from the opt-out (not the flags) and threads it into `runHereMode` + the greenfield path identically; **(4)** `serve` (`lib/server.js`) spawns a Conductor-default-on child (no `--monitor` threaded), the **"Monitor context (advanced)" web checkbox is RETIRED**, and the gauge polls/shows transparently whenever context data exists — `MMD_NO_AUTO_HANDOFF=1` (inherited via the `MMD_` env allowlist) opts the whole machine out. **Why the retired byte-for-byte-default-spawn contract is safe**: `stream-json` changes only the **OUTPUT FORMAT**, not the agent's work (the monitor re-renders human-readable progress); the bootstrap self-build runs monitored by default with the suite green + the result unaffected; the v0.13.1 **no-false-handoff** gate is preserved (a run that crosses the threshold but reaches no new boundary is never killed — proven by the AC-4 **default-on** integration tests: cooperative + enforce both reachable with NO flag); and the opt-out restores today's exact path. The **spawn-pin tests are INVERTED** (default has stream-json; the opt-out is byte-for-byte historical) so the contract is locked both directions. **Open question, noted (not blocking)**: whether the orchestrator genuinely saturates on a real slice is unmeasured (it delegates phases to fresh sub-agents → may stay light); default-on is SAFE regardless (the hybrid only acts at the threshold; below it it is inert + the negligible parse cost) and reversible. Deferred: removing the legacy flags (a future major), measuring saturation, `contextWindowFor('sonnet')` accuracy (separate small fix), parallel Conductor (D), Bundle C (E). See [ADR-054](./docs/adr/054-transparent-conductor-default.md).
- **Model-per-task — the Conductor allocates a model to each ROLE (v0.16.a)**: the model is no longer a single global default — each task runs on a model matched to its cognitive demand. Sébastien's design: *"dynamiquement en fonction des tâches à réaliser par les agents"* — the **orchestrator** mostly observes + delegates (it hands off at 70% so it never needs a 1M window), so it gets a LIGHT model; the **workers** that carry the real reasoning (spec design, implementation) get the STRONG model. **The gating fact, verified FIRST**: a per-subagent `model:` override is IGNORED when `claude -p` runs ATTACHED to a Claude Code host (sub-agents forced to Haiku — bug #47488) but is **HONORED when launched DETACHED via `setsid`** — exactly MMD's launch path (proven empirically: a pinned `model: opus` ran on `claude-opus-4-8[1m]` detached, Haiku attached) — so it is feasible in MMD's real path but **MUST be live-verified (AC-4)** because green fakes don't spawn real models. A new **pure** `lib/conductor/model-policy.js` (never throws): `modelForRole(role, env)` → a model alias or `null` (=CLI default) with cost-aware defaults (**orchestrator/review/judge/tester/unblock → `sonnet`, spec/impl → `opus`**); `MMD_MODEL_<ROLE>` (upper-cased role) overrides a role; an unknown/empty role → `null`; exports `ROLES` + `DEFAULTS` as the single source of truth. Applied at **two layers**: **L1** — MMD's OWN `claude -p` calls (`invokeJudge`/`invokeSealedTester` in `bin/mmd.js` + the `unblock` 5-Whys spawn in `lib/conductor/five-whys.js`) each pass `--model modelForRole(<role>)` (appended AFTER the positional prompt so the strict fake-claude arg contracts hold; omitted when the policy → null) — MMD owns each spawn so `--model` is honored directly. **L2** — the auto-dev PARENT is the `orchestrator` role: it gets `--model` via the existing `buildAutodevArgs` model path (resolution: `MMD_AUTODEV_MODEL` global override → policy → `sonnet`); the phase WORKERS are NAMED sub-agents materialized by `install-mmd.sh` (`.claude/agents/mmd-spec.md`/`mmd-impl.md`/`mmd-review.md` with a pinned `model:` frontmatter MIRRORING the policy DEFAULTS, each a faithful general-purpose agent PLUS the mandatory constitution-injection contract + fresh-context discipline), and the auto-dev workflow's phase invocations are rewritten from `subagent_type: "general-purpose"` to the named agents (spec→`mmd-spec`, impl→`mmd-impl`, reviewers→`mmd-review`); the per-phase checkpoint/handoff instructions (v0.12-v0.15) + fresh-context discipline are PRESERVED. **Reuse, not reinvention**: `buildAutodevArgs` itself is UNCHANGED (the orchestrator model is resolved by its caller), so the spawn-pin tests still hold; test-fixture mode is `[dream]` (no `--model`), so the fake-based suite is unaffected. **AC-4 is operator/live** — the run's `modelUsage` must show the orchestrator on its policy model AND ≥1 sub-agent on a distinct policy model (e.g. impl on opus while orchestrator on sonnet); a mismatch (all forced to one model) is a reported wall, NOT done ([[always-verify-live]], §VI). Deferred: truly adaptive (runtime complexity-driven) selection, a live env override for the L2 named agents, party-mode persona models, a per-run cost report. See [ADR-055](./docs/adr/055-model-per-task-role-policy.md).
- **Align the result to the ORIGINAL expectation — frozen oracle + dual-face verify (v0.17.0)**: the re-framed v0.6 — the CORE guarantee that the delivered result matches **what was originally asked**, and is corrected when it doesn't (Sébastien, verbatim: *"il faut trouver une solution où on s'aligne — ou on aligne le résultat — à ce qui était attendu à l'origine."*). Closes two drift holes in the v0.11 alignment path. **(1) The anchor was soft + driftable** — the judge graded against the mutable `.mmd/shared/slice.md`, which the build's own spec phase can POLISH, so the build could quietly **redefine its own success**. Fix: a new **pure** `lib/conductor/expectation.js` (`buildExpectationContent` builder + write-once `writeExpectation` over an injected fs — both never throw) writes **`.mmd/shared/expectation.md`** at run start (the original dream + Dream-Catcher scope when present), **ONCE, never overwritten** on a resume/re-run (the immutable oracle — the goalposts cannot move). The semantic judge is **re-anchored** to it (`resolveAlignmentAnchor`, pure, injected reader → the frozen oracle when present, else an HONEST fallback to the in-memory dream — never `slice.md`). **(2) The deterministic face was absent on `--here`** — Reality Check was a blanket SKIP, so nothing checked the change actually WORKS. Fix: `lib/reality-check.js` gains `hereRealityCheck` + a **technology-agnostic** `detectTestCommand` (npm/pytest/cargo/make — §VIII): in `--here` mode it runs the project's tests (red → FAIL) + a `run.json`-kind "does it run" check (cli → run the `run` command, exit 0; web-static → the existing open+screenshot+no-JS-error; service/other/none → honest SKIPPED, never a fabricated pass). **The dual-face gate** (`runAlignmentGate` in `bin/mmd.js`, reusing the v0.11 judge + iterate loop verbatim) runs **both faces** anchored to the frozen oracle: aligned only when BOTH pass; a gap on EITHER → the bounded `MMD_ALIGN_MAX_ITERS` iterate loop with feedback naming the failing face (pure `evaluateDeterministicFace`/`buildDeterministicFeedback`/`combineFaceFeedback`, never throw); unresolved after the cap → **exit 7** with `status.json.judge.face` ∈ `semantic`/`deterministic`/`both`, NOT done. **`MMD_SKIP_ALIGN=1` opts the WHOLE gate out** (back-compat — the standalone advisory Reality Check still runs on that path). Honest at every branch (§VI — never a fabricated pass). Zero new deps. The full polymorphic gStack catalogue (`/design-review`/`/cso`/`/canary` per deliverable type) + a full sealed-test suite on the normal path are deferred; a real `--here` run that iterates on a *deterministic* FAIL is the operator/live confirmation. See [ADR-056](./docs/adr/056-align-to-original-expectation.md).

## Repo-specific notes

- The workspace folder is `~/Documents/extend-bmad/` (will be renamed `make-my-dreams/` after v0.1 stabilization).
- The `mmdream` CLI entry point is `bin/mmd.js`.
- Tests are in `test/integration/` (52/52 passing as of v0.1.0).
- BMAD install (`_bmad/`), BMAD outputs (`_bmad-output/`), bmb/tea outputs (`skills/`), and `.claude/` (Claude Code project-scoped mirror) are all gitignored — regenerated by `install-mmd.sh`.
- **Keep the docs' mechanical meta honest.** Two commands refresh marker-bounded mechanical blocks without touching human prose: `mmdream handover` (HANDOVER.md's State block) and `mmdream document-readme` (README.md's Status + Changelog blocks, the latter built from git tag annotations). After a slice that bumps the version, adds an ADR/lesson, or ships a tag, run `mmdream document-readme --tests N` so the README's version/tag/counts/changelog don't drift (v0.3.d, ADR-025). Both never fabricate a number and never auto-commit.

---

*This file is part of MMD's constitution diffusion (Layer A). Last updated in `feat/constitution-diffusion` branch, after constitution v2.0 modularization.*
