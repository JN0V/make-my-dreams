<!-- GENERATED by `mmd test-health` — regenerate after material test changes; do NOT hand-edit. -->
# MMD Test Health

_MMD 0.8.0 · generated by `mmd test-health` — the Test Curator (polyglot, detect-and-report only)._

> Generated on demand by `mmd test-health` — the **Test Curator**: test-CORPUS health
> over time (the test analog of the Documentalist, for tests not docs). It is **detect-and-
> report only** — strictly read-only, it NEVER modifies a test. Every figure below is an
> **advisory heuristic**, not an authoritative audit. Distinct from `mmd qa` (per-change
> review) and the BMAD TEA (test architecture). POLYGLOT via per-technology adapters
> (§VIII) — the figures come from the adapter(s) matching the target stack. Regenerate
> after material test changes.

## Corpus

**1805 tests** across **189 files** (analyzed stack: JavaScript/TypeScript) (heuristic; discovered by the matching adapter(s)).

## Stratification distribution (testing.md §V)

| Stratum | Count | Share |
|---|---:|---:|
| `@smoke` | 9 | 0% |
| `@unit` | 1344 | 74% |
| `@integration` | 445 | 25% |
| `@e2e` | 2 | 0% |
| _untagged_ | 5 | 0% |

**Smoke health:** 9 `@smoke` tests — **within the testing.md §V fast-feedback band (5–10)**; looks usable. Advisory heuristic.

_Counts reflect the stratification value each adapter derived for its tests across ALL scanned files — not which subset your test runner actually executes (e.g. a smoke script may run only one directory). Advisory._

## Untagged tests (testing.md §V stratification violation)

⚠️ **5 untagged tests** — each violates the testing.md §V rule that every test belongs to exactly one stratum. Tag them (`@smoke`/`@unit`/`@integration`/`@e2e`). Advisory:

- `test/integration/test-health-polyglot.test.js:160` — plain
- `test/integration/test-health.test.js:56` — plainly untagged thing
- `test/unit/test-curator-adapters.test.js:93` — plainly untagged
- `test/unit/test-curator-scan.test.js:33` — plain title, no stratum
- `test/unit/test-curator-scan.test.js:160` — untagged c

## Oversized test files (split candidates)

_Threshold: > 500 lines OR > 60 test calls (advisory; override with `MMD_TEST_FILE_MAX_LINES` / `MMD_TEST_FILE_MAX_TESTS`)._

| File | Lines | Test calls | Over |
|---|---:|---:|---|
| `test/unit/argv-parser.test.js` | 640 | 82 | lines + tests |
| `test/integration/server.test.js` | 554 | 24 | lines |

_Split candidates — consider breaking each into focused sibling files. Advisory heuristic; large is not always wrong._

## Redundancy candidates (advisory — DETECT-BEFORE-CUT)

> **Heuristic, not an audit.** These are tests that *look* alike or that pile onto one module — worth a glance to see if the corpus can be pruned. A similar-looking test may still document a **distinct intent**, so nothing here is a deletion order: the Test Curator NEVER deletes a test — **the human decides**. Method: structural similarity (token-shingle Jaccard over each test body) + clustering by imported project target. NOT coverage (a coverage mode stays a deferred opt-in). Comparison is bounded to **within-file** (no global quadratic blow-up).

### Near-duplicate test pairs (similarity ≥ 0.9)

⚠️ **4 near-duplicate pairs** (within-file, similarity ≥ 0.9). Each pair MAY be two tests of the same thing — review whether one is redundant (it may not be). Advisory:

- `test/integration/unblock-dry-run.test.js:100` ↔ `test/integration/unblock-dry-run.test.js:143` — **0.95** similar
- `test/unit/sealed-tests-manifest.test.js:69` ↔ `test/unit/sealed-tests-manifest.test.js:82` — **0.94** similar
- `test/integration/here-mode-lib.test.js:43` ↔ `test/integration/here-mode-lib.test.js:115` — **0.92** similar
- `test/integration/server.test.js:194` ↔ `test/integration/server.test.js:240` — **0.91** similar

### Most-tested modules (over-test candidates)

_The modules with the most tests pointing at them. A large cluster is not wrong (a critical module SHOULD be well-tested) — it is just where redundancy, if any, is most likely. Advisory:_

| Target module | Tests | Files |
|---|---:|---:|
| `lib/invoke-autodev.js` | 204 | 33 |
| `lib/argv-parser.js` | 139 | 7 |
| `lib/server.js` | 110 | 6 |
| `lib/test-curator/scan.js` | 81 | 2 |
| `lib/composer/parse-lessons.js` | 62 | 7 |
| `lib/here-mode.js` | 48 | 5 |
| `lib/constitution-compose.js` | 39 | 4 |
| `bin/mmd.js` | 39 | 1 |
| `lib/test-curator/extract-bodies.js` | 39 | 1 |
| `lib/parse-dream.js` | 38 | 5 |
| `lib/conductor/stall-signals.js` | 38 | 2 |
| `lib/composer/match.js` | 36 | 4 |
| `lib/conductor/stall-detector.js` | 32 | 1 |
| `lib/discover/classify.js` | 28 | 2 |
| `lib/dream-catcher/elicit.js` | 27 | 3 |

_…and 78 more module(s) not listed (showing the top 15 by test count). Honest cap, not a silent truncation._
